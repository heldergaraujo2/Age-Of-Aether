# Phase 54 — Minimal Unreal Setup

1. Compile Editor/Game/Dedicated Server.
2. Create two real maps and register them in the existing map catalog.
3. Use one streaming link and call PrepareStream before activation.
4. Configure one performance budget.
5. Submit representative performance snapshots from the server/runtime integration.
6. Initialize the scale subsystem with one Dedicated Server node.
7. Register/unregister two test players and update load.
8. Test drain/resume and transfer contracts.

Do not create per-map managers or per-feature performance Blueprints.
